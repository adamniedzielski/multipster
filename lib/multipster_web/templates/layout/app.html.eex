<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Multipster - stay in touch with all your comrades</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="page-header">
        <h1>
          <%= link("Multipster", to: page_path(@conn, :index)) %>
        </h1>
        <p>
          stay in touch with all your comrades
        <p>
        <p>
          <%= if assigns[:current_user] do %>
            Signed in as
            <strong>
              <%= @current_user.email %>
            </strong>
            <%= link("Sign out", to: session_path(@conn, :delete), method: :delete) %>
          <% else %>
            <%= link("Sign in", to: sign_in_link_path(@conn, :new)) %>
            |
            <%= link("Register", to: account_path(@conn, :new)) %>
          <% end %>
        </p>
      </header>
      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert">
          <%= get_flash(@conn, :info) %>
        </p>
      <% end %>
      
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert">
          <%= get_flash(@conn, :error) %>
        </p>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
